<li>
	{{burger_name}}

{{#if devour}}
	<button class="eat-burger" data-id="{{id}}" data-newDevour="{{devour}}">
        Devour It!
	</button>
{{/if}}


	<button class="delete-burger" data-id="{{this.id}}">Delete Burger</button>
</li>
